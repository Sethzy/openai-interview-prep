{
  "name": "openai-interview-grinder",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "sync": "node scripts/sync-answer-bank.mjs",
    "dev": "npm run sync && vite",
    "build": "npm run sync && vite build && node scripts/prepare-sites.mjs",
    "preview": "vite preview"
  },
  "dependencies": {
    "ts-fsrs": "^5.2.3"
  },
  "devDependencies": {
    "vite": "^7.0.0"
  }
}
